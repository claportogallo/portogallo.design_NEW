Fix: enable page scroll for hero + projects
- Changed default: body { overflow: auto; } (was hidden) so you can scroll past the hero.
- Parallax hero + JS/CSS as before.
If you need to lock scroll during overlays later, we can toggle a .no-scroll class from JS.
